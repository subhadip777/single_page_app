//reducer //store component



export const initialState={
    Comments:COMMENTS,
    Leaders:LEADERS,
    Promotions:PROMOTIONS

}
export const Reducer=(state = initialState, acttion)=>{             //this Reducer function takes the current state and the actions and return the new state
        return state;
}